# Skill 评测平台需求文档

| 项 | 内容 |
|---|---|
| 文档版本 | v0.1（草稿，待评审） |
| 编写日期 | 2026-08-27 |
| 依据材料 | 《Skill 编写与开发测试规范》V1.0（2026年8月）；GitHub 同类项目调研（SkillLens / NVIDIA SkillEvaluator / promptfoo / Phoenix / Inspect AI 等）；三轮实测演示（静态检查、路由测试、A/B 对照） |
| 读者 | 处室接口人、测试人员、评审人员、平台开发团队 |
| 上游规范 | 本文档不重复规范条款，仅引用章节号。规范是"什么算合格"，本文档是"平台做什么" |

---

## 1. 背景与目标

### 1.1 背景

《Skill 编写与开发测试规范》定义了 Skill 上线的五层测试准入门禁（静态规范检查 → 路由测试 → 功能测试 → A/B 增益验证 → 安全审核）及版本回归要求。当前该流程依靠人工执行，存在四个问题：

1. 检查项散落在规范文档中，无机器可读规则源，执行口径因人而异；
2. 测试用例与评测记录无统一留存，回归测试无从复跑；
3. A/B 对照靠人脑记忆比对，原始证据易丢失；
4. 安全审核的 LLM 初审无工具承载，提示词模板无法固化。

### 1.2 平台定位

**面向企业内部的 Skill 上线评测门禁平台**：Skill 从注册到上线必须流经本平台的评测流水线，平台固化检查规则、沉淀测试资产、留存评测证据、支撑回归。

### 1.3 核心设计原则（已与需求方确认）

| # | 原则 | 说明 |
|---|---|---|
| P1 | **门禁 = 二元判定，不做评分** | 每个检查项结论只有"通过 / 整改"（及"需人工确认"）；不做 100 分制、等级、排行榜。理由：企业准入需要"缺哪几项必改"，分数引入噪音 |
| P2 | **确定性优先** | 能用规则判的绝不用 LLM；规则判 FAIL 的项，LLM 结论无权推翻。LLM 仅用于附录 C 安全初审等规则无法覆盖的主观判断 |
| P3 | **不用模型自报置信度** | 模型自评置信度无校准依据，不进准入链路。替代机制：证据来源二元标签（"文档可证" / "模型判断"），后者一律人工复核 |
| P4 | **标准前置** | A/B 观察点在运行前定义成"观察点卡"（可核对的 是/否/计数 项），不得在取得结果后变更指标 |
| P5 | **资产与记录分离** | 测试资产（用例、期望标签、观察点卡、Skill 版本）可复用；测试记录（原始输出、判定、耗时、环境快照）只增不改。回归 = 资产重放 + 记录对比 |
| P6 | **MVP 半自动** | A/B 双跑结果人工导入 + 平台并排对照展示；沙箱自动执行、CI、观测为二期 |
| P7 | **成本受控** | LLM 调用批量合并、内容哈希缓存、每日限额、超长输入降级；提供无 Key 的 mock 模式用于演示与冒烟 |

---

## 2. 用户与角色

| 角色 | 主要操作 | 对应规范 |
|---|---|---|
| Skill 作者 | 上传/注册 Skill、查看检查报告、整改后重新提交 | §12.1 |
| 测试人员 | 维护用例库、执行路由/功能测试、导入 A/B 双跑记录、填写观察点卡 | §10 |
| 评审人员 | 安全审核人工复核、A/B 结论评审、上线评审签字 | §9.2 / §12 |
| 处室接口人 | 查看本条线 Skill 清单与状态、导出注册清单 | §2.3 |
| 平台管理员 | 维护规则库与提示词模板、管理用户与条线、审计日志 | §12 |

---

## 3. 业务总流程

```
Skill 注册（版本 v 落库）
   │
   ├─① 静态规范检查（纯规则，秒级）───── 通过/整改
   ├─② 路由测试（规则断言自动判）────── 通过/整改
   ├─③ 功能测试（观察点卡，人工为主）── 通过/整改
   ├─④ A/B 增益验证（半自动+人工结论）─ 增益明显/有增益需优化/无明显增益
   ├─⑤ 安全审核（LLM 初审 + 人工复核）─ 通过/整改/高风险
   │
   ├─ 评审工作流（上线评审）────────── 通过 → 状态置 production
   │
   └─ 版本变更（1.0.2 → 1.1.0）
        └─ 回归测试：重放基线用例集 → 与基线记录 diff → 通过方可发布
```

状态机：`draft → testing → production → disabled`（与规范 §3.3 一致，状态变更同步注册清单）。

---

## 4. 功能需求

优先级定义：**P0** = MVP 必须；**P1** = MVP 后期或二期早期；**P2** = 二期。

### F1 Skill 注册与版本管理（P0）

| 需求 | 验收标准 |
|---|---|
| 上传 SKILL.md / skill 目录 / .zip | 三种形态均可注册；自动解析 frontmatter 与目录结构 |
| 元数据解析与展示 | 解析 name、description、metadata.version/author/owner/updated_at/status/tags 并展示 |
| 版本管理 | 每次注册生成不可变版本记录（版本号 + 内容 SHA-256 哈希 + 全量文件快照）；同一 name 可多版本并存 |
| 状态机 | draft/testing/production/disabled 四态流转留痕（谁、何时、为何）；状态与注册清单一致 |
| 注册清单 | 按条线（app/sys/net/fac/sec/cld/dev/adm）聚合展示全部 Skill 及状态，可导出 |

**工程注意事项**（实测踩坑）：解析与展示 Skill 内容时，必须剥离或转义 frontmatter 的 `---` 分隔符，防止下游（模板、提示词注入、Markdown 渲染）误解析。

### F2 静态规范检查引擎（P0，Tier 1）

**规则来源**：机器可读的规则库文件（单一事实源，参考 SkillLens rubric.yaml 模式），每条规则含：规则 ID、条款引用（规范章节号）、判定逻辑、级别（必须/应）、证据说明模板。规则库变更需版本化并留痕。

**规则构成**：规范条款代码化 + 以下 14 条净增硬检查（来自 SkillLens 源码分析，均已确认纳入）：

| 组 | 检查项 | 判定 | 级别 |
|---|---|---|---|
| 文档纪律 | description 长度预算 | 40~1024 字符内通过；超限或过短=整改 | 必须 |
| 文档纪律 | description 禁第一人称 | 出现"我将/我会/I will"=整改 | 必须 |
| 文档纪律 | SKILL.md 正文硬上限 | >6000 字符=整改（应拆入 references/） | 必须 |
| 文档纪律 | Markdown 结构完整 | 代码围栏未闭合、标题跳级=整改 | 必须 |
| 文档纪律 | 骨架章节齐全 | 附录 A 七章节按标题关键词机检（目标与边界/前置条件/设计模式/执行流程/异常与停止条件/资源导航/输出要求） | 必须 |
| 安全 | 危险命令词表扫描 | 命中 `rm -rf`、`DROP TABLE`、`push --force`、`--no-verify`、`reboot` 等词表且无警示/确认描述=整改；词表可由管理员扩充 | 必须 |
| 安全 | 密钥模式扫描 | `sk-`、`xoxb-`、`AIza`、`BEGIN PRIVATE KEY` 等=整改 | 必须 |
| 安全 | 生产地址/IP 硬编码扫描 | 内网 IP 段、生产域名模式=整改；模式库可配置 | 必须 |
| 依赖 | 依赖清单随包 | scripts/ 存在时必须含 requirements.txt / package.json / pyproject.toml 之一或显式声明零依赖 | 必须 |
| 输出 | 输出 Schema 必须存在 | 有交付物的 Skill 必须带 assets/*.schema.json（或等价字段表）且输出章节引用 | 必须 |
| 输出 | outputs 路径声明 | 输出要求章节须含 `/mnt/user-data/outputs` 与 `present_files` | 必须 |
| 输出 | 脚本参数契约 | scripts/* 须接受 --input/--output 或等价参数（静态扫描） | 必须 |
| 编排 | 子 skill 自洽 | Pipeline 型：每个子 SKILL.md 须含触发边界与流程章节 | 必须 |
| 复用 | 用例文件随包 | 必须提交 evals/ 用例集（输入+期望+断言，机器可读） | 必须 |

| 需求 | 验收标准 |
|---|---|
| 检查执行 | 注册/提交即自动执行全部静态检查，秒级返回 |
| 检查报告 | 每条规则输出：结论（通过/整改/需人工确认）+ 证据（命中位置/原文片段）+ 规范条款引用 + 整改建议 |
| "判不了"是合法结论 | 证据不足（如单文件上传无法判断目录结构）返回"需人工确认"，不得按通过或整改处理 |
| 阻断语义 | "必须"级规则 FAIL 阻断进入下一层；"应"级仅提示 |
| 规则等价性 | 规则库与代码行为一致（规则库改了，判定即变，无硬编码漂移） |

### F3 路由测试（P0）

| 需求 | 验收标准 |
|---|---|
| 用例库 | 按规范 §10.2 四类管理：正向（该触发必触发）、负向（不该触发必拒绝）、相邻（相近 Skill 边界）、边界/异常；每条含输入文本 + 期望标签（技能名或 NONE） |
| 测试执行 | 以注册清单中全部（或指定）Skill 的 name+description 构造发现层上下文，调用被测模型判定路由；期望标签匹配为纯规则断言，零 LLM 判分 |
| 变量固定 | 路由提示词作为受控版本资产单独管理；路由器指令变更与 Skill 变更不得混在同一次对比中 |
| 命中报告 | 汇总误触发（负向命中）、漏触发（正向未命中）、边界混淆（相邻误选）三类问题明细 |
| 客观上下文注入 | 执行时自动注入注册清单既有 Skill 的 name+description 作为对照上下文（防评审凭记忆） |
| 用例来源 | 支持三种入库：人工录入、AI 批量生成（必须人工审核后生效）、线上问题回流（P2） |

### F4 功能测试记录（P0）

| 需求 | 验收标准 |
|---|---|
| 观察点卡 | 按设计模式（Tool Wrapper/Generator/Reviewer/Inversion/Pipeline，规范 §10.4）预置观察点模板；每卡含可核对项（是/否/计数），运行前定义、运行后不可改 |
| 记录登记 | 测试人员录入/导入执行结果（原始输出、工具调用记录、耗时），逐项填写观察点核对值 |
| 证据留存 | 每条记录关联原始输入、原始输出、环境快照（模型、提示词版本、工具集与权限） |

### F5 A/B 增益验证（P0，半自动）

| 需求 | 验收标准 |
|---|---|
| 双跑记录导入 | A 组（不启用 Skill）/ B 组（启用 Skill）的原始输出、工具调用记录、耗时、成本可人工导入或粘贴入库 |
| 公平条件登记 | 记录并校验：同模型、同系统提示词、同工具集、同权限、同数据；Tool Wrapper 类 Skill 若目标工具不可用，B 组直接判功能失败，禁止与"无工具的 A 组"比较 |
| 并排对照视图 | 同任务 A/B 输出左右并排展示，支持差异高亮（参考 promptfoo 矩阵 + Different 过滤交互） |
| 断言预置 | 可为用例配置底线断言（关键词/Schema/自定义规则），规则自动判 |
| 观察点卡填报 | 按预定义观察点逐项填 是/否/计数，系统汇总各观察点 A/B 差异 |
| 三档结论 | 增益明显 / 有增益需优化 / 无明显增益，评审人签署；"无明显增益"必须触发处置动作（优化/合并/改形态/停建）选项 |
| 报告导出 | 按规范附录 B 格式导出 A/B 记录 |

### F6 安全审核（P0）

| 需求 | 验收标准 |
|---|---|
| LLM 初审 | 内置规范附录 C 提示词模板，对完整 Skill 目录（SKILL.md/references/scripts/assets）执行初审；模板版本化管理 |
| 完整性校验 | LLM 输出缺项（问题清单/需人工复核事项等结构不完整）= 整次初审失败重跑，不得按已有项放行 |
| 证据引用 | 每个问题项必须含：文件位置、风险等级、问题说明、证据（引用原文）、整改建议——缺证据的结论标记无效 |
| 人工复核 | LLM 初审结论一律为"辅助证据"；人工复核逐项确认/否决，最终结论三态（通过/整改/高风险）；高风险项强制说明 |
| 边界 | 确定性规则（F2）已 FAIL 的项，LLM 初审无权翻案，界面上固化展示 |
| 模型接入 | OpenAI 兼容网关（可配置 base_url/模型名），已验证 DeepSeek 可用 |

### F7 回归测试（P1，MVP 尾期）

| 需求 | 验收标准 |
|---|---|
| 基线管理 | 每个 Skill 指认"已验证基线"（版本 + 运行记录集合） |
| 回归触发 | 版本变更提交时，自动提示执行回归：重放基线用例集 |
| 四类必测 | 典型成功用例、关键边界用例、历史失败/整改用例（永久回归集）、受变更影响观察点（规范 §11.2） |
| Diff 报告 | 新旧两次记录逐用例对比：断言结果变化、输出差异、观察点变化；新增失败醒目标出 |
| 结论 | "结果无明显退化"方可进入上线评审；回归证据随版本存档 |

### F8 评测报告与证据中心（P0）

| 需求 | 验收标准 |
|---|---|
| 报告页 | 单次评测报告：检查结论汇总卡 + 分层明细（每条规则的证据卡）+ 整改清单（按条款引用排序） |
| 证据不可变 | 运行记录只增不改；报告与记录绑定（报告可追溯到原始证据） |
| 防伪 | 评测记录附带完整性哈希（规则库版本 + 输入内容哈希 + 结论哈希），可验证"报告出自哪套规则、哪份输入" |
| 导出 | 报告导出 Markdown / PDF；注册清单导出 |

### F9 评审工作流与状态流转（P1）

| 需求 | 验收标准 |
|---|---|
| 上线评审 | 汇聚五层结果 + 提交包材料（§12.1）形成评审单；评审人逐域签署（对应上线前检查清单十域） |
| 状态流转 | 评审通过与状态机联动（testing→production）；驳回附原因，回作者整改 |
| 全程留痕 | 所有状态变更、评审动作写审计日志 |

### F10 提交包与注册清单（P1）

| 需求 | 验收标准 |
|---|---|
| 提交包打包 | 一键导出 §12.1 材料：Skill 目录（校验无临时文件/秘密）、注册信息、测试记录、A/B 记录、安全审核、回归基线 |
| 注册清单同步 | 清单与元数据 status 联动，支持按条线/状态/版本筛选导出 |

### F11 CI 集成与沙箱执行（P2，二期）

- CLI / API 触发静态检查与路由测试，退出码作为门禁（规范 §11.3）；
- 沙箱（Docker）内执行 agent 双跑，自动生成 A/B 记录；
- 线上运行监控与问题回流（参考 Phoenix trace 结构，见 §6 数据模型）。

---

## 5. 非功能需求

| 类别 | 要求 |
|---|---|
| 性能 | 静态检查 ≤5s（常规包）；路由测试单用例 ≤ 模型网关超时；评测页面首屏 ≤2s |
| LLM 成本 | 批量合并调用（单次评测一次初审调用）；内容哈希缓存（同输入不重跑）；每用户每日调用限额（管理员可配）；超长输入自动降级并提示 |
| 可用性 | 单机部署（Docker Compose）；无外部网络依赖（模型走内部网关） |
| 安全 | API Key 仅服务端保存（加密存储，不入库明文、不写日志）；评测接口同源/令牌保护；操作审计日志；Skill 包静态扫描含密钥检测（F2） |
| 演示友好 | 无模型 Key 时 mock 模式可用（全流程可走通、界面明示 mock），用于培训与冒烟 |
| 兼容 | Skill 内容解析容错（frontmatter `---`、多规范 frontmatter 字段差异） |

---

## 6. 数据模型（五实体 + 记录结构）

| 实体 | 关键字段 | 说明 |
|---|---|---|
| SkillVersion | name、条线、version、status、content_hash、文件快照路径、created_by、parent_version | 不可变；哈希防篡改 |
| TestCase | 类型（正向/负向/相邻/边界/历史失败）、输入、期望标签、观察点卡引用、来源（人工/AI 生成/线上回流）、关联 skill、版本 | 测试资产；cases-as-code，可导出为 evals/ 文件随包 |
| RunRecord | run_id、skill_version_id、用例集版本、环境快照（模型/提示词版本/工具集权限）、逐用例：输入、原始输出、工具调用记录、耗时、token、成本、判定 | 只增不改；记录结构参考 Phoenix trace 字段（输入/输出/耗时/token/成本/父子关系/标注），为二期执行监控预留 |
| Baseline | skill_id、指向的 run_id 集合、确认人 | 回归对比基准 |
| ReviewRecord | 关联 run/版本、评审人、观察点卡填写值、三档结论、签署时间、审计字段 | A/B 结论与安全复核均落此实体 |

技术选型：MVP 用 SQLite + 文件系统（Skill 包原件、大 JSON 落文件，库内存路径与哈希）；量产后迁移 PostgreSQL。

---

## 7. 技术架构

```
┌─ 接入层 ── Web UI（Next.js App Router + TypeScript）
│            REST API（供二期 CLI/CI 复用）
├─ 应用层 ── FastAPI（Python）
│            ├─ 注册/版本/状态机服务
│            ├─ 静态检查引擎（规则库驱动，纯函数）
│            ├─ 路由测试执行器（网关调用 + 断言）
│            ├─ 安全审核服务（提示词模板 + 完整性校验）
│            └─ 报告/导出服务
├─ LLM 网关层 ── OpenAI 兼容统一接入（base_url 可配）、mock 降级、
│                内容缓存、限额、错误码归一（余额/鉴权/限流/超时/网络）
└─ 数据层 ── SQLite（→PostgreSQL）+ 文件存储（Skill 包 / 原始输出 JSON）
```

关键约束：
1. 静态检查引擎必须是**规则库驱动的纯函数**（规则文件 + 执行器分离），禁止把规则硬编码进业务代码；
2. 所有 LLM 调用必须经过统一网关层（缓存/限额/mock/错误归一在网关实现一次）；
3. 前端参考已验证的 SkillLens 报告页交互（结论卡+证据卡+整改清单）与 promptfoo 对照矩阵交互（并排输出+差异过滤）。

---

## 8. MVP 范围与开发顺序

| 阶段 | 交付 | 内容 | 验收要点 |
|---|---|---|---|
| **M1** | 注册 + 静态检查 + 报告 | F1、F2、F8（核心）、规则库（规范条款+14 条硬检查） | 上传即出检查报告；每条规则带证据与条款引用；"必须"级 FAIL 阻断 |
| **M2** | 安全审核 | F6 + LLM 网关层 | 附录 C 模板跑通；缺项失败重跑；人工复核流；mock 模式可用 |
| **M3** | 路由测试 | F3 + 用例库 | 三类用例可管理；断言自动判；误触发/漏触发报告 |
| **M4** | A/B 对照 | F5 + F4 | 双跑记录导入；并排对照；观察点卡填报；三档结论 |
| **M5** | 回归 + 工作流 | F7、F9、F10 | 基线 diff 报告；状态流转全链路；提交包导出 |
| 二期 | CI / 沙箱 / 回流 | F11 | 退出码门禁；沙箱双跑自动化；线上监控 |

**MVP 明确不做**（防止范围蔓延）：评分与排行榜、模型自报置信度、自动 LLM 判分（A/B 裁决一律人工）、沙箱执行、生产观测、多租户。

---

## 9. 里程碑验收（平台级）

1. 用一个真实 Skill（如 sys-redis-memory-diagnose 样例）走通 注册→静态→安全审核→路由→A/B→上线评审 全流程，每层证据可追溯；
2. 该 Skill 升一个版本，回归测试能复放基线用例并产出 diff 报告；
3. 无模型 Key 时全流程 mock 可走通；
4. 注册清单导出与规范 §12.1 提交包格式一致。

---

## 10. 风险与开放问题

| # | 风险/问题 | 对策/待决 |
|---|---|---|
| R1 | 危险命令词表、生产地址模式的误报 | 词表可配置 + 命中结果一律带证据，允许人工标记误报并回流规则库 |
| R2 | 路由测试的"期望标签"依赖发现层提示词写法 | 提示词作为受控版本资产；对比实验固定单一变量 |
| R3 | A/B 人工导入的记录不规范 | 导入模板强校验（必填环境快照字段） |
| R4 | 附录 C 提示词在不同模型上的输出稳定性 | M2 阶段用 2~3 个模型实测；必要时按模型微调模板并版本化 |
| R5 | 用例库冷启动为空 | M3 交付时预置三类种子用例模板 + AI 生成（人工审核）入口 |
| O1 | 规范 v1.1 若调整检查条款，规则库如何同步 | 规则库带条款引用字段，规范变更→规则库 diff 审核流（P2） |
| O2 | 是否引入 LLM 裁判做 A/B 预筛 | 二期评估，先立"人工终审"流程 |

---

## 附录 A：参考项目与借鉴点映射

| 项目 | 借鉴 | 扬弃 |
|---|---|---|
| SkillLens | 规则/LLM 双轨、rubric 单一事实源、证据卡交互、mock 模式、网关防护（缓存/限额/同源/错误归一） | 不采用打分制、市场/商业维度、模型自报置信度；注意 frontmatter 解析坑 |
| NVIDIA SkillEvaluator | 分层门禁、阻断/建议分级、用例随包（evals/）、"缺证据=校验不完整" | 三期沙箱执行时再参考 Harbor |
| promptfoo | 断言库（标签匹配/JSON 校验/自定义函数）、A/B 矩阵与 Different 过滤交互、SQLite 评测存储模型 | 不引入其 CLI 为主的使用方式 |
| Phoenix | 运行记录的 trace 字段结构（输入/输出/耗时/token/成本/父子/标注） | MVP 不部署观测平台 |
| Inspect AI | Task=Dataset+Solver+Scorer 抽象 | 二期沙箱执行参考 |

## 附录 B：观察点卡模板（规范 §10.4 数字化）

```
观察点卡
├─ 关联：Skill 名称/版本、设计模式、用例 ID
├─ 预定义观察点（运行前锁定，每项为 是/否/计数）
│   例（Tool Wrapper）：① 是否先调用 get_instance 取实例标识？
│                      ② 时间窗参数是否来自用户输入或前序结果？
│                      ③ 鉴权失败时是否停止（是/否）
│   例（Generator）：① 必填字段缺失数（计数） ② 是否按模板输出（是/否）
├─ 公平条件快照：模型 / 系统提示词版本 / 工具集与权限 / 数据条件
└─ 结论：增益明显 / 有增益需优化 / 无明显增益（附处置选项）＋评审人
```
